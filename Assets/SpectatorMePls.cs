using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpectatorMePls : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(("nada aq"));
    }
}
